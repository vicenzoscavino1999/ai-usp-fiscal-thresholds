# AI-USP Fiscal Thresholds

Reproduction package scaffold for the AI-USP fiscal threshold framework.

Stage 1 implements only the clean repository layout, Docker/package infrastructure,
pre-registration hash, pilot ingestion scripts, and frozen snapshot manifest.
The deterministic model engine and result verification are intentionally not
implemented in this stage.

